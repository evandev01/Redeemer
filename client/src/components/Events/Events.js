import React, { Fragment } from 'react';
import Header from '../Header';

const Events = () => {
    return (
        <Fragment>
            <Header />
        </Fragment>
    )
};

export default Events;