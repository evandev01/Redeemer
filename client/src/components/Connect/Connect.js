import React, { Fragment } from 'react';
import Header from '../Header';

const Connect = () => {
    return (
        <Fragment>
            <Header />
        </Fragment>
    )
};

export default Connect;